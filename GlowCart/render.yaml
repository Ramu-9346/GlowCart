services:
  - type: web
    name: glowcart
    env: dotnet
    buildCommand: "dotnet restore && dotnet publish -c Release -o out"
    startCommand: "dotnet out/GlowCart.dll"
    plan: free
    region: singapore
